{
  "name": "coffee-order-backend",
  "version": "1.0.0",
  "description": "Coffee Order App Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "psql -U postgres -d coffee_order -f database/schema.sql",
    "db:seed": "psql -U postgres -d coffee_order -f database/seed.sql"
  },
  "keywords": ["coffee", "order", "express", "postgresql"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
